## Get comfortable with class components by turning a functional component into one

In the `react` folder run `npm i` and then `npm run dev`.

This ReactJS app currently uses functional components.
Convert this functional component into a ReactJS **Class Component**.
After the conversion the component should have identical functionality.
