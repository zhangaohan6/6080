Change the commented code of `src/App.tsx` to switch between the two components.

- see the file `src/components/RouterApp.tsx` for a solution using `react-router`
- see the file `src/components/StateApp.tsx` for a solution using React state
